import { FormatPromedioPipe } from './format-promedio.pipe';

describe('FormatPromedioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPromedioPipe();
    expect(pipe).toBeTruthy();
  });
});
